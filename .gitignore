# OS / editor
.DS_Store
Thumbs.db
*.swp

# Claude Code session metadata (keep the skill, ignore local state)
.claude/settings.json
.claude/settings.local.json
.claude/projects/
.claude/sessions/
.claude/shell-snapshots/
.claude/todos/

# Python (Plan C fallback)
__pycache__/
*.pyc
.venv/
venv/
*.env
.env

# Hook patterns file (local only — never commit)
.githooks/.blocked

# Scratch
*.tmp
gathered.json
eval.json
